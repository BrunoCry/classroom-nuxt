version: '3.7'

services:
  app:
    build: .
    container_name: nuxt-app
    restart: always
    ports:
      - '3000:3000'
    command: 'npm run start'
    env_file: .env

  nginx:
    build: ./nginx
    ports:
      - '80:80'
    depends_on:
      - app